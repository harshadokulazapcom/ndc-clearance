name: Deploy Site to Netlify

on:
  push:
    branches:
      - main
    paths:
      - 'site/**'
      - 'ndc_schemas/**'
      - 'ndc_content/**'
      - '.github/workflows/deploy-site.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: main
    defaults:
      run:
        working-directory: site

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: site/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: Build site
        run: npm run build
        env:
          PUBLIC_API_URL: https://mcp-ndc.sunrisehikers.io

      - name: Deploy to Netlify
        run: |
          npm install netlify-cli -g
          netlify deploy --dir=dist --prod --message "Deploy from GitHub Actions" --no-build
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
